# 休肝日戻し時の飲酒関連フィールドクリア修正 - 2026年03月14日 10:00

## 作業内容
「今日は飲む」で杯数を宣言した後、「今日は飲まない（休肝日）」に変更しても、declaredLimit等の飲酒関連フィールドがリセットされないバグを修正。statusを「kyukan」に戻す全3箇所で、飲酒関連フィールドもnullにリセットするようにした。

## 変更ファイル
- app/(tabs)/index.tsx
- app/declaration.tsx
- app/confirm-drink.tsx

## 変更詳細

### 3ファイル共通の修正パターン
statusを「kyukan」に変更する際、飲酒関連フィールド（declaredLimit, drinkingReason, actualDrinks, satisfaction, alternativeAction）をnullにリセットするよう追加。

### 変更箇所

#### app/(tabs)/index.tsx - handleNodrink()（98行目）
```typescript
// 変更前
await patchRecord(today, { status: "kyukan" });

// 変更後
await patchRecord(today, {
  status: "kyukan",
  declaredLimit: null,
  drinkingReason: null,
  actualDrinks: null,
  satisfaction: null,
  alternativeAction: null,
});
```

#### app/declaration.tsx - handleCancel()（55行目）
```typescript
// 同上のパターン
```

#### app/confirm-drink.tsx - handleNoDrink()（87行目）
```typescript
// 同上のパターン
```

## 影響範囲
- ホーム画面の「今日は飲まない」ボタン
- 飲酒確認画面の「やっぱり飲まない」ボタン
- 宣言画面の「やっぱり今日は飲まない」ボタン
- 記録画面での表示（宣言した上限が正しくクリアされる）

## テスト項目
1. pnpm check - 型エラーなし ✅
2. pnpm test - 全テストパス ✅
3. 手動確認: 「今日は飲む」→杯数設定→「今日は飲まない」→記録画面で宣言が表示されないこと
